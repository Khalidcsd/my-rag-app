run = "pip install -r requirements.txt && uvicorn app:app --host 0.0.0.0 --port 3000"
entrypoint = "app.py"
modules = ["python-3.11"]

[nix]
channel = "stable-25_05"
packages = ["bash", "libxcrypt"]

[unitTest]
language = "python3"

[gitHubImport]
requiredFiles = [".replit", "replit.nix"]

[deployment]
run = "pip install -r requirements.txt && uvicorn app:app --host 0.0.0.0 --port 3000"

[agent]
expertMode = true

[[ports]]
localPort = 3000
externalPort = 80
